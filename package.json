{
  "name": "delta-truth-engine",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "description": "\u0394 Truth Engine \u2014 RTPTPA verification API + Canva-design reverse-proxy on delta.genesisconductor.io",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler pages dev",
    "deploy": "node build.mjs && wrangler pages deploy",
    "migrate:local": "wrangler d1 migrations apply delta-truth-ledger --local",
    "migrate:remote": "wrangler d1 migrations apply delta-truth-ledger --remote",
    "build": "node build.mjs"
  }
}
